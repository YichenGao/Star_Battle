import assert from 'assert';
//import { WebServer } from '../src/WebServer';
import fetch from 'node-fetch';
import HttpStatus from 'http-status-codes';

describe('server',function(){
    it('covers puzzle file with valid puzzle inside');
    it('covers puzzle file with grammatically invalid puzzle inside');
    it('covers puzzle file with grammatically valid but structurally invalid puzzle inside');
});